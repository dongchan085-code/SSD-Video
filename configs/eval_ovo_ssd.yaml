# OVO-Bench Evaluation Configuration - SSD Model
# Inherits eval_ovo_base.yaml; only the LoRA-merged-specific bits differ.

extends: eval_ovo_base.yaml

model:
  model_path: ./outputs/lora_checkpoint/merged
  use_lora: false
  is_merged: true

evaluation:
  compare_with_base: true
